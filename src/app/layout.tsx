import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { AuthProvider } from "@/lib/authx";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "QRGen Pro - QR Generator SaaS Indonesia",
  description: "QR Generator SaaS terlengkap di Indonesia dengan fitur custom, dynamic, analytics, dan bulk generation. Cocok untuk bisnis, event, dan marketing.",
  keywords: ["QR Code", "QR Generator", "SaaS", "Indonesia", "Dynamic QR", "Analytics", "Bulk QR"],
  authors: [{ name: "QRGen Pro Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <AuthProvider>
          {children}
          <Toaster />
        </AuthProvider>
      </body>
    </html>
  );
}
